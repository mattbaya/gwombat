# GWOMBAT Configuration Template
# Copy this file to local-config/.env and customize for your environment
# Or run ./shared-utilities/setup_wizard.sh for interactive configuration

# IMPORTANT: Never commit the actual .env file to version control!
# The setup wizard will create local-config/.env with your actual values

# =============================================
# DOMAIN & ORGANIZATION CONFIGURATION
# =============================================

# Your Google Workspace domain
DOMAIN="your-domain.edu"

# Admin account for Google Workspace operations
ADMIN_USER="admin@your-domain.edu"

# User account for testing and operations
USER_ACCOUNT="user@your-domain.edu"

# =============================================
# ORGANIZATIONAL UNITS
# =============================================

# Suspended account organizational units
SUSPENDED_OU="/Suspended Users"
PENDING_DELETION_OU="/Suspended Users/Pending Deletion"
TEMPORARY_HOLD_OU="/Suspended Users/Temporary Hold"
ALUMNI_OU="/Suspended Users/Alumni"

# =============================================
# EXTERNAL TOOLS CONFIGURATION
# =============================================

# GAM (Google Apps Manager) - Primary tool for Google Workspace
GAM_PATH="/usr/local/bin/gam"

# GYB (Got Your Back) - Gmail backup tool
GYB_PATH="/usr/local/bin/gyb"

# rclone - Cloud storage synchronization
RCLONE_PATH="/usr/local/bin/rclone"

# =============================================
# PYTHON ENVIRONMENT
# =============================================

# Python executable path
PYTHON_PATH="/usr/local/bin/python3"

# Virtual environment path (will be created by setup wizard)
VENV_PATH="./python-modules/venv"

# =============================================
# SERVER & DEPLOYMENT CONFIGURATION
# =============================================

# Production server (for deployment)
PRODUCTION_SERVER="your-server.edu"

# SSH key for deployment
SSH_KEY_PATH="$HOME/.ssh/gwombatgit-key"

# Deployment path on production server
DEPLOYMENT_PATH="/opt/gwombat"

# =============================================
# DATABASE CONFIGURATION
# =============================================

# Main database path (auto-configured)
DATABASE_PATH="./local-config/gwombat.db"

# Menu database path (auto-configured)
MENU_DB_PATH="./local-config/menu.db"

# =============================================
# LOGGING CONFIGURATION
# =============================================

# Log directory
LOG_DIR="./logs"

# Log level (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL="INFO"

# =============================================
# INITIAL SETUP CONFIGURATION
# =============================================

# Run initial scans on first setup
RUN_INITIAL_SCANS="no"
SCAN_SUSPENDED_ACCOUNTS="no"
SCAN_USER_STORAGE="no"
SCAN_GROUPS="no"

# Setup completion flags (set by setup wizard)
SETTINGS_CONFIGURED="false"
SETUP_COMPLETE="false"
SKIP_FIRST_TIME_SETUP="false"